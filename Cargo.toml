[package]
name = "bpf-verifier"
version = "0.1.0"
edition = "2024"

[dependencies]
object = { version = "0.36", features = ["read", "write", "elf"] }
clap = { version = "4", features = ["derive"] }
thiserror = "2"
tempfile = "3"

[dev-dependencies]
assert_cmd = "2"
